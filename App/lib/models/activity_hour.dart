class ActivityHour {
  const ActivityHour({
    required this.label,
    required this.steps,
    required this.activeMinutes,
  });

  final String label;
  final int steps;
  final int activeMinutes;
}
